services:
  backend:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:Z
      - server_node_modules:/app/node_modules:Z

volumes:
  server_node_modules: